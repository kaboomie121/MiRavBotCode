import logging
logging.getLogger(__name__)
logging.info('Importing XXXXXREPLACETHISXXXXX.py')

from json import loads
from pathlib import Path
base_path = Path(__file__).parent
config = loads((base_path / "../config.json").read_text())

import discord